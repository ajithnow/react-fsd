---
description: How to add a new FSD feature module (config, routes, sidebar, permissions)
globs: src/features/**/*
alwaysApply: false
---

# Add a feature module

Full checklist: `.agents/rules/add-feature.md`  
Skill: `.agents/skills/add-feature/SKILL.md` · CRUD: `.agents/skills/add-crud-page/SKILL.md`

## Discovery

`src/main.tsx` loads every `src/features/*/config.ts` via `import.meta.glob`.  
Adding `config.ts` is enough — no edits to `main.tsx`.

## Required layout

```text
src/features/<name>/
  config.ts
  constants/
  routes/
  pages/
  locales/en.json + de.json + index.ts
  sidebar.ts
  index.ts
```

Reference: `src/features/bookings/` (scaffold) · `src/features/users/` (CRUD).

## `config.ts`

```ts
const config: FeatureConfig = {
  routes: featureRoutes,
  locales: { ns: '<name>', resources: featureLocales },
  sidebar: featureSidebar,
}
export default config
```

## Rules

1. Feature owns routes, permission strings, and sidebar.
2. Session permissions from `/api/auth/me` only — permission-based (not roles).
3. Gate routes with `hasPermission` + redirect to `/`. UI: `<Can>` or `useRBAC()`.
4. Sidebar `permission` filtered at assembly; honors `VITE_RBAC_ENABLED=false`.
5. Locales via `config.locales` with feature `ns`.
