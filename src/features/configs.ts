export const supportedLanguages = ['en', 'de'];
export const features = [
  'auth',
  'dashboard',
  'home',
  'users',
  'shared',
  'settings'
];

const moduleConfig = {
  supportedLanguages,
  features,
};

export default moduleConfig;
